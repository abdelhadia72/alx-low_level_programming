#include <stdio.h>

/**
 * main - Entry point
 * @argc: The argument count
 * @argv: The argument vector
 * Return: (0)
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", *argv);
	return (0);
}
