#include "main.h"
/**
 * mul - function for multiplcation
 * Description: This function thaks two arguments
 * and multly them .
 * @a: is the argument for fist number
 * @b: is the argument for 2ed number
 * Return: multiplyed resualt
 */

int mul(int a, int b)
{
	return (a * b);
}
